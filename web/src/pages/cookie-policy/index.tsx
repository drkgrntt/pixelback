import { NextPage } from 'next'
import styles from './CookiePolicy.module.scss'

interface Props {}

const CookiePolicy: NextPage<Props> = (props) => {
  return null
  return (
    <div className={styles.wrapper}>
      <h1>Cookie Policy</h1>
    </div>
  )
}

export default CookiePolicy
